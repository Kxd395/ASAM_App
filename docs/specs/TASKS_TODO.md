# Agent tasks

1. Implement NavigationSplitView shell and the 14 screens.
2. Build safety banner and gates. Do not lock forms. Export is hard blocked until action recorded.
3. Implement Problems module as single CRUD owner. Remove add problem from domains.
4. Implement EMR Context drawer as read only with TTL policy.
5. Build PDFComposer and dynamic renderers for D1 substances, D2 meds, Problems list.
6. Add Preflight checks and neutral filename policy.
7. Implement upload with retry and Binary then DocumentReference fallback on 413.
8. Add unit tests: gates, hash stability, dynamic renderer overflow, preflight.
